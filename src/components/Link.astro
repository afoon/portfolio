---
import "./Link.css";

const { url, variant } = Astro.props;
const isButton = Boolean(variant == "button");
---

<span
  ><div class:list={["link-container", { button: variant }]}>
    <a class="link-text" href={url}>
      <slot />
    </a>
  </div></span
>
